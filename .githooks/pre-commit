#!/bin/sh
# DevBox pre-commit hook
set -e
PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$PROJECT_ROOT"

node "$PROJECT_ROOT/node_modules/@askend-lab/devbox/packages/hooks/src/cli/index.js" run pre-commit
