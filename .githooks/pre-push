#!/bin/sh
# DevBox pre-push hook
set -e
PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$PROJECT_ROOT"

node "/home/alex/users/boxer/devbox/packages/hooks/src/cli/index.js" run pre-push
npm test
