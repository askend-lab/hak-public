{
  "name": "audio-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --testPathIgnorePatterns=test/features --json --outputFile=jest-results.json",
    "test:e2e": "NODE_OPTIONS=--experimental-vm-modules jest --testPathPattern=test/features --json --outputFile=jest-results.json",
    "test:coverage": "jest --coverage --json --outputFile=jest-results.json",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3",
    "axios": "^1.6.2",
    "@types/axios": "^0.14.0",
    "serverless": "^3.38.0",
    "serverless-esbuild": "^1.52.0",
    "esbuild": "^0.20.0",
    "jest-cucumber": "^3.0.1"
  },
  "dependencies": {
    "@hak/shared": "workspace:*",
    "@aws-sdk/client-s3": "^3.478.0",
    "@aws-sdk/client-sqs": "^3.478.0"
  }
}
