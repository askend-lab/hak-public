{
  "name": "@hak/merlin-worker",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "echo 'Python package — run pytest tests/ -v'",
    "test:full": "echo 'Python package — run pytest tests/ -v'"
  }
}
