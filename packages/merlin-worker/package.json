{
  "name": "@hak/merlin-worker",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "echo 'Python package — run pytest tests/ -v'",
    "test:full": "if [ -f .venv/bin/pytest ]; then .venv/bin/pytest tests/ -v --tb=short; else echo 'Skip: .venv not set up (run: python3 -m venv .venv && .venv/bin/pip install -r requirements-test.txt)'; fi"
  }
}
